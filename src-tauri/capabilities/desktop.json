{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-autostart",
  "description": "Desktop-only: launch-on-login so opted-in users still get reminders after a reboot. Mobile platforms are excluded so the Android build never sees these permissions.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
